If your PC has the Nvidia GPU - replace the kernel-params.nix with the kernel-params-nvidia.nix and uncomment needed lines.
But in general I suggest to find kernel params for YOUR configuration or paste them from the clean configuration.nix, it is important to have kernel parameters dedicated to your hardware.
